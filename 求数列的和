#include<stdio.h>
#include<math.h>
int main() {
    int i;
    double m=0, n=0, sum=0;
    while (scanf("%lf%lf", &n, &m)!=EOF) {
        for (i=0;i<m;i++) {
            sum += n;
            n = sqrt(n);
        }
        printf("%.2f\n", sum);
        sum = 0;
    }
    return 0;
}
